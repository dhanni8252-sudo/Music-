PremiumMusicBot v7 PRO

Credentials set — seedha run karo:
  pip install -r requirements.txt
  python bot.py

Session (pehli baar):
  Bot DM → /start → Session → Phone → OTP → Done!

Commands:
  /play <song>  /search <song>
  /pause /resume /skip /stop /queue /np
